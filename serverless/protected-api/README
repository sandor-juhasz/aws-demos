This project implements a serverless RESTful API which is protected
with IAM authorization. Only an AWS user with the proper access policy
can access the API.


sam init --runtime python3.7 -n protected-api
